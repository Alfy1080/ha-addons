#include <tunables/global>

profile ha_addon_unpackerr flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  
  # Allow network access and file system operations
  network inet stream,
  network inet6 stream,
}