#include <tunables/global>

profile ha_addon_honeygain flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  capability,
  network,
  signal,
  unix,
  ipc,
  ptrace,
  mount,
  remount,
  umount,
}